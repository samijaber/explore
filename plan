- prevent a photo to reappear
- find better API calls to make:
  - users.photo: 1 from same user,
  - search.photos: 1 from same query,
  - search.collections: 1 from a collection with that query,
  - ^^ 1 from same collection
  DEPRECATE:
  - categories.categoryPhoto: 1 in same category,



query made:
  - get 10 photos by query (search.photos)
  - get collections by query (search.collections)
    - get photos from the first couple collections

  - mix & match 3 of them




given a selected picture:
  - get another pic by same user
  - get a pic liked by same user
  - get another pic liked/by the same user

  - get randoms for any failure ^
